# Note

Some JavaScript tasks have similar HTML files because the main focus of those exercises is the JavaScript logic.

To avoid repeating unnecessary HTML code, I used a simple and consistent page structure and implemented the required functionality in separate JavaScript files for each task.

Tasks involving DOM manipulation, forms, event handling, and jQuery contain additional HTML elements as required by the exercise.
